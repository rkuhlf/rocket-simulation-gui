// Handle which window should be shown at which time

// currentWindow = "home"
// Might be totally unnecessary. I din't realize that a tags were just links

function changeWindow(newWindow) {
    window.loadFile('./html/' + newWindow)
}