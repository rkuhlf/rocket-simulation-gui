doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")

        base(href="../")
        link(rel="stylesheet", href="./css/main.css")
        title Analyze and Simulate Flight
    body 
        include ./helpers

        //- When there is no past data, that is what the page should say, and the center should be largely occupied by a button that says simulate rocket

        +menuButton